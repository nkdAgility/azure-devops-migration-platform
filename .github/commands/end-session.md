Invoke the [end-session skill](./../skills/end-session/SKILL.md).

Verify all gates are clear (Reviewer approved, all tests passing, no TODOs), finalise the session log, and signal readiness to commit.
