Invoke the [start-session skill](./../skills/start-session/SKILL.md).

Assemble session context, confirm the requirement is scoped to one scenario, generate a session ID, write the session start log, and invoke the Specification Agent.
