Invoke the [review skill](./../skills/review/SKILL.md).

Verify pre-conditions (all tests passing, no pending steps), assemble the review package, invoke the Reviewer Agent, record the verdict, and either proceed to `/end-session` or return required changes to the Implementer Agent.
