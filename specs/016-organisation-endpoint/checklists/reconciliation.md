# Reconciliation Checklist — 016-organisation-endpoint

- [x] All task lines in `tasks.md` have a final status marker.
- [x] Superseded tasks reference explicit supersession sources.
- [x] Core endpoint type artifacts exist in current source tree.
- [x] Legacy `DiscoveryJobOrganisation` and `DiscoveryJobAuthentication` references are removed from `src/`.
- [ ] `docs/module-development-guide.md` and `docs/capabilities-guide.md` explicitly document endpoint parameter convention (`T021`).
- [ ] Full unfiltered `dotnet test` evidence captured for this reconciliation run (`T025`).
- [ ] Scenario/system execution evidence captured for queue export ADO path (`T026`).
